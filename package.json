{
  "name": "store-service",
  "version": "1.0.2",
  "description": "A service-based Redux slice augmentation and alternative to redux-toolkit",
  "main": "lib/index.js",
  "scripts": {
    "test": "node test",
    "start": "node index",
    "build": "tsc -b",
    "build-test": "tsc -b && node test"
  },
  "author": "Dale Blackwood",
  "license": "MIT",
  "dependencies": {
    "redux": "^4.0.5"
  }
}
